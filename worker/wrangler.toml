name = "portfolio-api"
main = "src/index.js"
compatibility_date = "2024-12-01"

# ---------------------------------------------------------------------------
# PUBLIC vars only (safe-ish to commit). Origins for CORS.
# SECRETS — never put here. Use:
#   npx wrangler secret put DATABASE_URL
#   npx wrangler secret put JWT_SECRET
#   npx wrangler secret put ADMIN_PATH    (optional)
# ---------------------------------------------------------------------------

[vars]
ALLOWED_ORIGIN = "https://a2mbd3.pages.dev"
